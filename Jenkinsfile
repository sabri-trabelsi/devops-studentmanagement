pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo '🎉 WEBHOOK FONCTIONNE !'
                sh 'date'
                sh 'ls -la'
            }
        }
    }
}
